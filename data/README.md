# Data

Local cache and generated data used by the wiki viva kit.

Files under `data/raw` and `data/derived` are cache surfaces and are generally
not versioned. Versioned manifests may live here when they are needed to point
the wiki to canonical external artifacts such as Drive files.

# Gate Core

Deterministic proposal-gate state machine used by the wiki CLIs.

The package validates approved proposal states and keeps human review as the
boundary between generated ingestion output and committed memory.

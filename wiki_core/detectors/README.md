# Detectors

Deterministic scanners for access secrets, sensitive public-boundary data and
known entity patterns.

Access secrets are blocked everywhere. Personal data is only evaluated at the
configured public boundary.

## Project Overlay Notes

- This open-source overlay is intentionally generic. Downstream private repos can
  add local examples, extra fields or stricter correspondence rules without
  changing the core template.

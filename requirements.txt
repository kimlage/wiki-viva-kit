# Living wiki kit dependencies (wiki_core/ + scripts/wiki_*.py).
# Install: pip install -r requirements.txt
#
# SYSTEM dependency (not pip-installable): poppler (the `pdftotext` binary).
#   macOS:  brew install poppler
#   debian: apt-get install poppler-utils
# Without poppler, PDF extraction emits a `pdftotext_unavailable` warning and
# returns empty text (never raw bytes), instead of polluting the index.

PyYAML>=6.0          # wiki.config.yaml and frontmatter parsing
openpyxl>=3.1        # .xlsx/.xlsm spreadsheet extraction
python-docx>=1.1     # .docx document extraction

# Dev/test
pytest>=8.0
hypothesis>=6.0     # property-based parser parity; required so CI cannot skip the module
jsonschema>=4.21    # executable contract checks for published JSON schemas

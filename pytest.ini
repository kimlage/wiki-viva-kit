[pytest]
# Collect only the official suite: keeps a bare `pytest` from importing
# legacy demos under archive/ (ImportError from stale dependencies).
testpaths = tests
markers =
    retired: historical certification-state-machine coverage awaiting deletion in phase 2
